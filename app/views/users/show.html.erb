<!-- This page displays the user's profile page, which contains static information like their date of birth, location etc. -->

<% if @successfulSignin and current_user.username == @user.username  %>
    Welcome, <strong><%= @user.username %></strong>
<% @successfulSignin = false %>
  </div>

<% end %>
<div class="page-header">
	<h1><%= @user.username %></h1>
	<%= link_to "Services offered", "/users/#{@user.id}/offered" %>
</div>

<br>


